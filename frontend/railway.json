{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "npx vite preview --port ${PORT:-5173} --host 0.0.0.0",
    "restartPolicyMaxRetries": 5
  }
}
